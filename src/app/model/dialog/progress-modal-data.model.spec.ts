import { ProgressModalData } from './progress-modal-data.model';

describe('ProgressModalData', () => {
  it('should create an instance', () => {
    expect(new ProgressModalData()).toBeTruthy();
  });
});
